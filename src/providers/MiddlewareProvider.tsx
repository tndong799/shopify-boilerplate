/* eslint-disable @typescript-eslint/no-explicit-any */

const MiddlewareProvider: React.FC<{ children: React.ReactElement }> = ({
    children,
}) => {
    return <>{children}</>;
};

export default MiddlewareProvider;
